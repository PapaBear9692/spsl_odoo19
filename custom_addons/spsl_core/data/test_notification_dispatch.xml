<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Server Action to Test Notification Dispatch - using res.users model -->
        <record id="action_test_notification_dispatch" model="ir.actions.server">
            <field name="name">Test Notification Dispatch</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="state">code</field>
            <field name="code"># Test notification dispatch
# Use current user as document
document = env.user
result = env['spsl.notification.dispatcher'].dispatch('NE-TEST-001', document)
print('Dispatch result:', result)
</field>
        </record>
    </data>
</odoo>